#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL);
    ios_base::sync_with_stdio(0);

    int a, b;
    while (cin >> a >> b) {
        cout << a + b << "\n";
    }
    return 0;
}